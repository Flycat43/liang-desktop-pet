name: Plugin Check

on:
  push:
    branches: [main]
    paths:
      - "plugins/liang-harness-plugin/**"
      - "scripts/prepare-plugin-release.mjs"
      - "package.json"
      - ".github/workflows/plugin-check.yml"
      - ".github/workflows/release.yml"
  pull_request:
    paths:
      - "plugins/liang-harness-plugin/**"
      - "scripts/prepare-plugin-release.mjs"
      - "package.json"
      - ".github/workflows/plugin-check.yml"
      - ".github/workflows/release.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: plugins/liang-harness-plugin
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: plugins/liang-harness-plugin/package-lock.json
      - run: npm ci --legacy-peer-deps
      - run: npm test
      - run: npm pack --dry-run
      - run: mkdir -p "$RUNNER_TEMP/liang-plugin-release"
      - run: npm pack --pack-destination "$RUNNER_TEMP/liang-plugin-release"
      - run: npm run release:plugin:prepare -- "$RUNNER_TEMP/liang-plugin-release"
        working-directory: .
